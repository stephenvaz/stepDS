#include<stdio.h>
#include<stdlib.h>

struct  node
{
    int data;
    struct node *next;
    struct node *prev;
}*new, *head, *tail, *curr, *temp;

typedef struct node* NODE;

Node


int main()
{
    
    return 0;
}